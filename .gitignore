# Dependencias y build
node_modules/
dist/

# Secretos / entorno local (nunca subir)
.env
.env.*
!.env.example

# Skills y tooling local de agentes (no forman parte del entregable)
.agents/
.codex/

# Cache / temporales
*.log
.DS_Store
Thumbs.db
